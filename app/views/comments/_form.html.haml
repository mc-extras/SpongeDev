= form_for([@plugin, @plugin.comments.build]) do |f|
  %p
    = f.label :content
    %br/
    = f.text_area :content, class: 'form-control', rows: 6
  %p
    = f.submit 'Comment', class: 'btn btn-primary', data: { disable_with: "Submitting..." }