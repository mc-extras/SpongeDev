.container 
  = render 'context'
  %table.table.table-bordered.table-striped
    %thead
      %th Plugin
      %th Download Name
      %th MC Version
      %td File
      %td{colspan: 2}  
    %tbody
      - if @files.any?
        - @files.each do |file|
          %tr
            %td= link_to file.plugin.name, file.plugin
            %td= file.name
            %td= file.mc_version
            %td= link_to file.file.file.filename, file.file.url
            %td{align: "center"}= link_to 'Approve', plugin_plugin_file_approve_path(file.plugin, file)
            %td{align: "center"}= link_to 'Deny', plugin_plugin_file_deny_path(file.plugin, file)
      - else
        %tr
          %td{:align => "center", :colspan => "5", style: "background-color: white; color: darkgray;"} No downloads need to be moderated.